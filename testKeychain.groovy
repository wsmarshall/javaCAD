//a combination key holder and bottle opener 

CSG makeKeychain(){
//set up parameters
	//default total length of the key holder
	length = new LengthParameter("total length of the piece", 10, [100, 1.0])
	//default thickness (depth)

	//default width

//computation of constants from parameters

//shape constructions for individual sections

//assembly of the whole thing


}
